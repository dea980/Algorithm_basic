import math 
def solution(flo):
    ## 이경우 내장함수가 여럿 잇다
    ### round(), trunc(),  floor()
    # case 1 round()
    # return round(flo)
    # case 2 trunc() import math
    # return math.trunc(flo)
    # case 3 floor()
    return math.floor(flo)